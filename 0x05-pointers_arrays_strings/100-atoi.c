#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _atoi - Function that converts strings to integers
 *
 * @s - input string
 *
 * Return: Non.
 */

int _atoi(char *s)
{
	int x;
	char str

	*s = str;
       	x = atoi(*s);
}
